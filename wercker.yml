box: wercker/nodejs
# Build definition
build:
  # The steps that will be executed on build
  steps:
    # A step that executes `npm install` command
    - npm-install
    # A step that executes `npm test` command
    - npm-test

    # A step that will run pre-build grunt tasks
    - grunt:
        tasks: less uglify

    # A custom script step, name value is used in the UI
    # and the code value contains the command that get executed
    - script:
        name: wintersmith build
        code: ./node_modules/.bin/wintersmith build -o ./build
deploy:
  steps:
    - script:
        name: Deploy to Github Personal Page
        code: |-

            git config --global user.email "pleasemailus@wercker.com"
            git config --global user.name "wercker"

            # remove current .git folder
            rm -rf .git

            git init
            git add .
            git commit -m "deploy commit from $WERCKER_STARTED_BY"
            git push -f https://$WERCKER_GH_TOKEN@github.com/joemercer.github.io.git master:gh-pages